#!/bin/sh

echo "This is a `basename $0` script."
echo "Its purpose is to finish automatic (non-interactive) setup of the"
echo "working directory 'first'."

echo ""

echo "The `basename $0` script received the following arguments:"

for OPTION
do
  echo "'"${OPTION}"'"
done
