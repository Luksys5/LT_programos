#!/bin/sh

case "$1" in
    *.gz) unpack='gzip -dc' ;;
    *.Z)  unpack='gzip -dc' ;;
    *)    unpack='cat' ;;
esac

${unpack} ${1+"$@"} \
| perl -pe 's/[\000-\037]?\000\001\215/\n/g' \
| perl -ne 'print if /Rfactor v Resolution/../^\s*$/ or /Completeness v Resolution/../^\s*$/'
