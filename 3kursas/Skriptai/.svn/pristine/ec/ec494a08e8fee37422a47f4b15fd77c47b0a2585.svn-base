#!/usr/bin/perl -w

use strict;

my $prev = "";

sub mod24 { my $x = shift;  $x - int($x) + $x % (60 * 24) }

while(<>) {
   my @line = split;
   my @time = split(":", $line[7] );
   my $time = $time[0] * 60 + $time[1];
   if( $prev ne "" ) {
#      printf "%6.1f  ", $time;
      printf "%6.1f\n", mod24($time - $prev);
   }
   $prev = $time;
}
