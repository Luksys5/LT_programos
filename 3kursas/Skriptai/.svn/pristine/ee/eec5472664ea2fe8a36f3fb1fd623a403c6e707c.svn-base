#!/bin/sh

case `uname` in
    OSF1)  SORT_COL="+3" ;;
    IRIX*) SORT_COL="+3" ;;
    *)     SORT_COL="+3" ;;
esac

df -k \
| sort -nr ${SORT_COL} \
| awk '{
	printf "%-25s ",$1;
	for(i=2;i<=NF;i++) {
	    printf "%-12s ",$i
	};
	printf "\n"
       }'
