Po instaliacijos ir Github registracijos dar butina github'o tinklapy sukurit direktorija!
git config --global user.name "YOUR NAME"
git config --global user.email "YOUR EMAIL ADDRESS"

git init <- makes your repozitory in working dir
git clone http:/// <--parsiuncia visa info is repoz
Jau atsikelus repozitorija
git checkout <-- pakeicia saka (branches)
git add <-- ideda faila
git commit <-- commitina faila
git push <-- nusiuncia i repozitorija faila(us)
git pull <-- parsiuncia is dabartines sakos faila(us)
git checkout --orphan <branchname> <-- sukuria nauja saka
--------------------arba
git branch -b "branchname"
rm -rf * | git rm -rf *
git push origin "branchname"
Creates new branch with name "branchname"

git rm --cached -r . <- istrina darb dir
tik po add commit ir push komandu matoma pazanga tinklapyje

git ls-remote --heads <- Shows all branches.
